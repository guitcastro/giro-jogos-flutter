{
  "name": "firestore-rules-test",
  "version": "1.0.0",
  "description": "Testes automatizados das regras do Firestore do projeto giro-jogos-flutter",
  "main": "firestore.rules.test.js",
  "scripts": {
    "test": "mocha *.test.js --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^10.0.0",
    "mocha": "^11.0.0"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "node-fetch": "^3.3.2"
  }
}
